import Background from '../../component/Background';
import Modal from '../../component/Modal';

const Signin = () => {
  return (
    <Background>
      <Modal />
    </Background>
  );
};

export default Signin;
